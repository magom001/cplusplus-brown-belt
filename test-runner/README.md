# C++ Testiing Framework

A simple C++ testing framework with AssertEqual(x, y) and Assert(bool) functions.

## How to use

Create an instance of TestRunner. Call RunTest with a function containing your asserts as method param.

